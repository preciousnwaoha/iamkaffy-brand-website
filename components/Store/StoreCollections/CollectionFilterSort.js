import React from 'react'
import classes from "./CollectionFilterSort.module.css"

const CollectionFilterSort = () => {
  return (
    <div className={classes["collection-filter-sort"]}>
        <div className={classes["filter-wrapper"]}>

        </div>
        <div className={classes["sort-wrapper"]}>

        </div>
    </div>
  )
}

export default CollectionFilterSort